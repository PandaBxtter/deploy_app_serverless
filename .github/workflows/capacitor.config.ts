import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.editorapp',
  appName: 'AscentCode',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
